Para testar o código você tem algumas alternativas:
    - Rodar ele dentro da IDE (no meu caso eu compilo no Intellij);
    - Executar pelo CMD com o comando 'javac', que seria basicamente 'javac ServidorWebSimples.java' e ele executa.
        - Isso tem que ser feito dentro da pasta em que se encontra o arquivo, então attention ao cd..

No terminal ficará as mensagens de log, contendo o acesso, uma identidade que pode ser o IP e o horário registrado.